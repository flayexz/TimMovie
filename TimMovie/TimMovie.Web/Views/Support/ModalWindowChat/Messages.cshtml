@model IEnumerable<TimMovie.Core.Classes.MessageWithContent>
@{
    Layout = null;
}

@foreach (var message in Model)
{
    await Html.RenderPartialAsync("~/Views/Support/ModalWindowChat/Message.cshtml", message);
}