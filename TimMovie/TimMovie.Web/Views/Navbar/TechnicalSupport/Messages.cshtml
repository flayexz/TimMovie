@model IEnumerable<TimMovie.Core.Classes.MessageWithContent>

@{
    Layout = null;
}

@foreach (var message in Model)
{
    await Html.RenderPartialAsync("~/Views/Navbar/TechnicalSupport/Message.cshtml", message);
}