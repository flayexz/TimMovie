@model List<TimMovie.Web.ViewModels.FilmCard.BigFilmCardViewModel>
@{
    ViewBag.Title = "Смотреть позже";
}

@foreach (var card in Model)
{
    await Html.RenderPartialAsync("~/Views/Partials/FilmCard/BigFilmCard.cshtml", card);
}
